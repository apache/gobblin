package gobblin.ingestion.google.adwords;

import java.util.Properties;

import org.testng.annotations.Test;


@Test(groups = {"gobblin.ingestion.google.adwords"})
public class GoogleAdWordsSourceTest {
  @Test
  public void main()
      throws Exception {

//    Properties configurations = null;//GoogleAdWordsSource.getConfigurations();
//    String property = configurations.getProperty(GoogleAdWordsSource.KEY_GET_REFRESH_TOKEN);
//    boolean getRefreshToken = false;
//    if (property != null) {
//      getRefreshToken = Boolean.valueOf(property);
//    }
//
//    GoogleAdWordsCredential credential = new GoogleAdWordsCredential(configurations);
//    if (getRefreshToken) {
//      //LOG.info(String.format("Your refresh token is: %s", credential.getRefreshToken()));
//    } else {
//      new GoogleAdWordsExtractor(configurations, credential).work();
//    }
  }
}
